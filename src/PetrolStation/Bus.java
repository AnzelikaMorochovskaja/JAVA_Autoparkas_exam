package PetrolStation;

public class Bus extends AutoVehicle {

    public Bus(String carNum, int fuelLeft, int tankCapacity, FuelType fuelType) {
        super(carNum, fuelLeft, tankCapacity, fuelType);
    }
}
